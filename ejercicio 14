#include <stdio.h>

int main ()

{
int f,conv;

printf("Introduzca grados fahrenheit que quieras convertir:");
scanf("%d",&f);

conv=(f-32)*5/9;

printf("Convertido en grados centigrados es: %d",conv);
return 0;
}
